# delvop v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a keyboard-driven TUI that manages multiple terminal coding agents (Claude Code, Codex, Gemini) via tmux, with a dashboard view, permission approve/deny, notifications, and cost tracking.

**Architecture:** Go + Bubbletea TUI wrapping tmux sessions. Each agent runs in an isolated tmux session. A provider interface abstracts agent-specific behavior (state parsing, hooks, cost). A session manager coordinates lifecycle. A Unix socket receives hook events. The TUI polls tmux panes as fallback state detection.

**Tech Stack:** Go 1.22+, Bubbletea (github.com/charmbracelet/bubbletea), Bubbles, Lip Gloss, Cobra, go-sqlite3, BurntSushi/toml, tmux

---

## Prerequisites

Install dependencies before starting:

```bash
brew install go tmux
```

---

## File Map

| File | Responsibility |
|------|---------------|
| `go.mod`, `go.sum` | Module definition, dependencies |
| `main.go` | Entry point, calls `cmd/delvop.Execute()` |
| `cmd/delvop/root.go` | Cobra root command, subcommands, wires everything |
| `internal/config/config.go` | TOML config parsing, defaults, paths |
| `internal/provider/provider.go` | `AgentProvider` interface + registry |
| `internal/provider/claude.go` | Claude Code provider implementation |
| `internal/provider/codex.go` | Codex CLI provider (stub) |
| `internal/provider/gemini.go` | Gemini CLI provider (stub) |
| `internal/session/types.go` | Session, Event, PermissionRequest structs |
| `internal/session/tmux.go` | Tmux bridge: create, capture, send-keys, kill |
| `internal/session/manager.go` | Session CRUD, state polling, event log |
| `internal/hooks/engine.go` | Unix socket listener, JSON event dispatch |
| `internal/notify/router.go` | Native OS notifications, sound, debouncing |
| `internal/cost/tracker.go` | Per-session cost aggregation |
| `internal/cost/store.go` | SQLite persistence for cost data |
| `internal/tui/messages.go` | Custom Bubbletea message types |
| `internal/tui/styles/theme.go` | Tokyo Night color palette, semantic styles |
| `internal/tui/keys.go` | Key binding definitions |
| `internal/tui/model.go` | Main Bubbletea model, Init, subscriptions |
| `internal/tui/update.go` | Update function, message routing |
| `internal/tui/view.go` | All rendering: dashboard, focused, status bar |
| `internal/templates/loader.go` | Template discovery + TOML parsing |
| `internal/templates/builtin/solo.toml` | Single-agent template |
| `internal/templates/builtin/fullstack.toml` | Multi-agent template |
| `Makefile` | Build, install, clean targets |
| `.gitignore` | Go ignores |

---

## Task 1: Project Scaffold + Dependencies

**Files:**
- Create: `go.mod`, `main.go`, `cmd/delvop/root.go`, `Makefile`, `.gitignore`

- [ ] **Step 1: Initialize git repo**

```bash
cd /Users/meher/delvop
git init
```

- [ ] **Step 2: Create .gitignore**

```gitignore
# Binaries
delvop
*.exe
*.dll
*.so
*.dylib

# Test
*.test
*.out
coverage.txt

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Build
dist/
```

- [ ] **Step 3: Initialize Go module**

```bash
go mod init github.com/meher/delvop
```

- [ ] **Step 4: Create main.go**

```go
package main

import (
	"fmt"
	"os"

	"github.com/meher/delvop/cmd/delvop"
)

func main() {
	if err := delvop.Execute(); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}
```

- [ ] **Step 5: Create cmd/delvop/root.go**

```go
package delvop

import (
	"github.com/spf13/cobra"
)

var rootCmd = &cobra.Command{
	Use:   "delvop",
	Short: "Engineering Command Center for Terminal Coding Agents",
	Long:  "Manage a team of AI coding agents from a single terminal dashboard.",
}

func Execute() error {
	return rootCmd.Execute()
}
```

- [ ] **Step 6: Create Makefile**

```makefile
.PHONY: build install clean run

BINARY=delvop

build:
	go build -o $(BINARY) .

install: build
	cp $(BINARY) $(GOPATH)/bin/$(BINARY) 2>/dev/null || cp $(BINARY) ~/go/bin/$(BINARY)

clean:
	rm -f $(BINARY)

run: build
	./$(BINARY)
```

- [ ] **Step 7: Install dependencies + verify build**

```bash
go get github.com/spf13/cobra@latest
go mod tidy
go build .
```

Expected: Binary `delvop` created, runs and shows help text.

- [ ] **Step 8: Commit**

```bash
git add .
git commit -m "feat: project scaffold with cobra CLI"
```

---

## Task 2: Config System

**Files:**
- Create: `internal/config/config.go`
- Test: `internal/config/config_test.go`

- [ ] **Step 1: Write config test**

```go
package config

import (
	"os"
	"path/filepath"
	"testing"
)

func TestDefaultConfig(t *testing.T) {
	cfg := Default()
	if cfg.General.DefaultProvider != "claude" {
		t.Errorf("expected default provider 'claude', got %q", cfg.General.DefaultProvider)
	}
	if cfg.General.PollIntervalMs != 500 {
		t.Errorf("expected poll interval 500, got %d", cfg.General.PollIntervalMs)
	}
	if cfg.Tmux.Prefix != "dv-" {
		t.Errorf("expected tmux prefix 'dv-', got %q", cfg.Tmux.Prefix)
	}
	if cfg.Notify.DebounceMs != 500 {
		t.Errorf("expected debounce 500, got %d", cfg.Notify.DebounceMs)
	}
}

func TestLoadFromFile(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "config.toml")
	err := os.WriteFile(path, []byte(`
[general]
default_provider = "gemini"
default_model = "2.5-pro"
poll_interval_ms = 1000

[tmux]
prefix = "test-"
`), 0644)
	if err != nil {
		t.Fatal(err)
	}

	cfg, err := LoadFrom(path)
	if err != nil {
		t.Fatal(err)
	}
	if cfg.General.DefaultProvider != "gemini" {
		t.Errorf("expected provider 'gemini', got %q", cfg.General.DefaultProvider)
	}
	if cfg.General.DefaultModel != "2.5-pro" {
		t.Errorf("expected model '2.5-pro', got %q", cfg.General.DefaultModel)
	}
	if cfg.General.PollIntervalMs != 1000 {
		t.Errorf("expected poll 1000, got %d", cfg.General.PollIntervalMs)
	}
	if cfg.Tmux.Prefix != "test-" {
		t.Errorf("expected prefix 'test-', got %q", cfg.Tmux.Prefix)
	}
	// Non-specified fields should have defaults
	if cfg.Notify.DebounceMs != 500 {
		t.Errorf("expected default debounce 500, got %d", cfg.Notify.DebounceMs)
	}
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/config/ -v
```

Expected: FAIL — package doesn't exist yet.

- [ ] **Step 3: Implement config.go**

```go
package config

import (
	"os"
	"path/filepath"

	"github.com/BurntSushi/toml"
)

type Config struct {
	General  GeneralConfig  `toml:"general"`
	Tmux     TmuxConfig     `toml:"tmux"`
	Worktree WorktreeConfig `toml:"worktree"`
	Notify   NotifyConfig   `toml:"notify"`
	Cost     CostConfig     `toml:"cost"`
}

type GeneralConfig struct {
	DefaultProvider  string `toml:"default_provider"`
	DefaultModel     string `toml:"default_model"`
	AutoCompact      bool   `toml:"auto_compact"`
	CompactThreshold int    `toml:"compact_threshold"`
	PollIntervalMs   int    `toml:"poll_interval_ms"`
}

type TmuxConfig struct {
	Prefix string `toml:"prefix"`
}

type WorktreeConfig struct {
	Enabled     bool   `toml:"enabled"`
	BaseDir     string `toml:"base_dir"`
	AutoCleanup bool   `toml:"auto_cleanup"`
}

type NotifyConfig struct {
	Channels      []string    `toml:"channels"`
	FocusSuppress bool        `toml:"focus_suppress"`
	DebounceMs    int         `toml:"debounce_ms"`
	Sound         SoundConfig `toml:"sound"`
}

type SoundConfig struct {
	InputNeeded string `toml:"input_needed"`
	TaskDone    string `toml:"task_done"`
	Error       string `toml:"error"`
}

type CostConfig struct {
	DailyBudget float64 `toml:"daily_budget"`
	LogFile     string  `toml:"log_file"`
}

func Default() *Config {
	return &Config{
		General: GeneralConfig{
			DefaultProvider:  "claude",
			DefaultModel:     "opus",
			AutoCompact:      true,
			CompactThreshold: 80,
			PollIntervalMs:   500,
		},
		Tmux: TmuxConfig{
			Prefix: "dv-",
		},
		Worktree: WorktreeConfig{
			Enabled:     false,
			BaseDir:     ".worktrees",
			AutoCleanup: true,
		},
		Notify: NotifyConfig{
			Channels:      []string{"native", "sound"},
			FocusSuppress: true,
			DebounceMs:    500,
			Sound: SoundConfig{
				InputNeeded: "Basso",
				TaskDone:    "Glass",
				Error:       "Sosumi",
			},
		},
		Cost: CostConfig{
			DailyBudget: 50.0,
			LogFile:     "~/.delvop/cost_log.jsonl",
		},
	}
}

func configDir() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".delvop")
}

func DefaultPath() string {
	return filepath.Join(configDir(), "config.toml")
}

func Load() (*Config, error) {
	return LoadFrom(DefaultPath())
}

func LoadFrom(path string) (*Config, error) {
	cfg := Default()
	data, err := os.ReadFile(path)
	if err != nil {
		if os.IsNotExist(err) {
			return cfg, nil
		}
		return nil, err
	}
	if _, err := toml.Decode(string(data), cfg); err != nil {
		return nil, err
	}
	return cfg, nil
}
```

- [ ] **Step 4: Install toml dependency + run tests**

```bash
go get github.com/BurntSushi/toml@latest
go mod tidy
go test ./internal/config/ -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/config/ go.mod go.sum
git commit -m "feat: TOML config system with defaults"
```

---

## Task 3: Provider Interface + Claude Provider

**Files:**
- Create: `internal/provider/provider.go`, `internal/provider/claude.go`, `internal/provider/codex.go`, `internal/provider/gemini.go`
- Test: `internal/provider/provider_test.go`, `internal/provider/claude_test.go`

- [ ] **Step 1: Write provider registry test**

```go
package provider

import "testing"

func TestRegistryAndLookup(t *testing.T) {
	// Registry is populated by init() in provider files
	p, err := Get("claude")
	if err != nil {
		t.Fatalf("expected claude provider, got error: %v", err)
	}
	if p.Name() != "Claude Code" {
		t.Errorf("expected name 'Claude Code', got %q", p.Name())
	}

	_, err = Get("nonexistent")
	if err == nil {
		t.Error("expected error for nonexistent provider")
	}

	names := List()
	if len(names) < 1 {
		t.Error("expected at least 1 registered provider")
	}
}
```

- [ ] **Step 2: Write Claude provider test**

```go
package provider

import (
	"strings"
	"testing"
)

func TestClaudeParseState(t *testing.T) {
	claude := &Claude{}

	tests := []struct {
		name    string
		content string
		want    AgentState
	}{
		{"idle prompt", "$ ", StateIdle},
		{"working spinner", "⠋ Working on task...", StateWorking},
		{"permission prompt", "Do you want to allow this action? (y/n)", StateWaitingForPermission},
		{"empty", "", StateUnknown},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := claude.ParseState(tt.content)
			if got != tt.want {
				t.Errorf("ParseState(%q) = %v, want %v", tt.content, got, tt.want)
			}
		})
	}
}

func TestClaudeLaunchCmd(t *testing.T) {
	claude := &Claude{}
	cmd := claude.LaunchCmd("opus")
	if !strings.Contains(cmd, "claude") {
		t.Errorf("expected 'claude' in launch cmd, got %q", cmd)
	}
}
```

- [ ] **Step 3: Run tests, verify they fail**

```bash
go test ./internal/provider/ -v
```

Expected: FAIL

- [ ] **Step 4: Implement provider.go (interface + registry)**

```go
package provider

import (
	"fmt"
	"sort"
	"sync"
)

type AgentState int

const (
	StateUnknown AgentState = iota
	StateIdle
	StateWorking
	StateWaitingForPermission
	StateDone
	StateError
)

func (s AgentState) String() string {
	switch s {
	case StateIdle:
		return "IDLE"
	case StateWorking:
		return "WORKING"
	case StateWaitingForPermission:
		return "NEEDS INPUT"
	case StateDone:
		return "DONE"
	case StateError:
		return "ERROR"
	default:
		return "UNKNOWN"
	}
}

type PermissionRequest struct {
	Tool    string
	File    string
	Lines   string
	Summary string
	Diff    string
}

type AgentProvider interface {
	Name() string
	LaunchCmd(model string) string
	InjectHooks(workDir, sessionID, socketPath string) error
	ParseState(paneContent string) AgentState
	ParsePermission(paneContent string) *PermissionRequest
	ParseCost(paneContent string) (costUSD float64, tokIn, tokOut int64)
	CompactCmd() string
	ApproveKey() string
	DenyKey() string
}

var (
	registry = make(map[string]AgentProvider)
	mu       sync.RWMutex
)

func Register(name string, p AgentProvider) {
	mu.Lock()
	defer mu.Unlock()
	registry[name] = p
}

func Get(name string) (AgentProvider, error) {
	mu.RLock()
	defer mu.RUnlock()
	p, ok := registry[name]
	if !ok {
		return nil, fmt.Errorf("unknown provider: %s (available: %v)", name, List())
	}
	return p, nil
}

func List() []string {
	mu.RLock()
	defer mu.RUnlock()
	names := make([]string, 0, len(registry))
	for name := range registry {
		names = append(names, name)
	}
	sort.Strings(names)
	return names
}
```

- [ ] **Step 5: Implement claude.go**

```go
package provider

import (
	"strings"
)

type Claude struct{}

func init() {
	Register("claude", &Claude{})
}

func (c *Claude) Name() string { return "Claude Code" }

func (c *Claude) LaunchCmd(model string) string {
	if model != "" {
		return "claude --model " + model
	}
	return "claude"
}

func (c *Claude) InjectHooks(workDir, sessionID, socketPath string) error {
	// v0.1: no hook injection yet, rely on polling
	return nil
}

func (c *Claude) ParseState(paneContent string) AgentState {
	if paneContent == "" {
		return StateUnknown
	}
	lines := strings.Split(strings.TrimSpace(paneContent), "\n")
	last := strings.TrimSpace(lines[len(lines)-1])

	// Permission prompts
	lower := strings.ToLower(paneContent)
	if strings.Contains(lower, "allow") && (strings.Contains(lower, "(y/n)") || strings.Contains(lower, "yes") || strings.Contains(lower, "deny")) {
		return StateWaitingForPermission
	}
	if strings.Contains(lower, "do you want to") || strings.Contains(lower, "permission") {
		return StateWaitingForPermission
	}

	// Working indicators
	if strings.ContainsAny(last, "⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏") {
		return StateWorking
	}
	if strings.HasPrefix(last, "Working") || strings.HasPrefix(last, "Reading") ||
		strings.HasPrefix(last, "Writing") || strings.HasPrefix(last, "Searching") {
		return StateWorking
	}

	// Idle / prompt
	if strings.HasSuffix(last, "$ ") || strings.HasSuffix(last, "% ") || last == "$" || last == "%" {
		return StateIdle
	}
	if strings.Contains(last, ">") && len(last) < 20 {
		return StateIdle
	}

	return StateWorking
}

func (c *Claude) ParsePermission(paneContent string) *PermissionRequest {
	lower := strings.ToLower(paneContent)
	if !strings.Contains(lower, "allow") && !strings.Contains(lower, "permission") {
		return nil
	}
	// Extract basic info from pane content
	req := &PermissionRequest{
		Summary: strings.TrimSpace(paneContent),
	}
	// Try to find tool name
	for _, tool := range []string{"Write", "Edit", "Bash", "Read", "Glob", "Grep"} {
		if strings.Contains(paneContent, tool) {
			req.Tool = tool
			break
		}
	}
	return req
}

func (c *Claude) ParseCost(paneContent string) (float64, int64, int64) {
	// v0.1: basic cost parsing - will be improved
	// Claude Code shows cost in the status line like "$2.14"
	return 0, 0, 0
}

func (c *Claude) CompactCmd() string { return "/compact" }
func (c *Claude) ApproveKey() string { return "y" }
func (c *Claude) DenyKey() string    { return "n" }
```

- [ ] **Step 6: Implement codex.go and gemini.go stubs**

codex.go:
```go
package provider

type Codex struct{}

func init() { Register("codex", &Codex{}) }

func (c *Codex) Name() string                  { return "Codex CLI" }
func (c *Codex) LaunchCmd(model string) string  { return "codex" }
func (c *Codex) InjectHooks(_, _, _ string) error { return nil }
func (c *Codex) ParseState(content string) AgentState {
	return StateUnknown
}
func (c *Codex) ParsePermission(_ string) *PermissionRequest { return nil }
func (c *Codex) ParseCost(_ string) (float64, int64, int64)  { return 0, 0, 0 }
func (c *Codex) CompactCmd() string                          { return "" }
func (c *Codex) ApproveKey() string                          { return "y" }
func (c *Codex) DenyKey() string                             { return "n" }
```

gemini.go:
```go
package provider

type Gemini struct{}

func init() { Register("gemini", &Gemini{}) }

func (g *Gemini) Name() string                  { return "Gemini CLI" }
func (g *Gemini) LaunchCmd(model string) string  { return "gemini" }
func (g *Gemini) InjectHooks(_, _, _ string) error { return nil }
func (g *Gemini) ParseState(content string) AgentState {
	return StateUnknown
}
func (g *Gemini) ParsePermission(_ string) *PermissionRequest { return nil }
func (g *Gemini) ParseCost(_ string) (float64, int64, int64)  { return 0, 0, 0 }
func (g *Gemini) CompactCmd() string                          { return "" }
func (g *Gemini) ApproveKey() string                          { return "y" }
func (g *Gemini) DenyKey() string                             { return "n" }
```

- [ ] **Step 7: Run tests**

```bash
go test ./internal/provider/ -v
```

Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add internal/provider/
git commit -m "feat: agent provider interface with claude/codex/gemini"
```

---

## Task 4: Session Types + Tmux Bridge

**Files:**
- Create: `internal/session/types.go`, `internal/session/tmux.go`
- Test: `internal/session/tmux_test.go`

- [ ] **Step 1: Create types.go**

```go
package session

import (
	"time"

	"github.com/meher/delvop/internal/provider"
)

type Session struct {
	ID           string
	Name         string
	TmuxSession  string
	Provider     provider.AgentProvider
	ProviderName string
	Model        string
	WorkDir      string
	Branch       string
	State        provider.AgentState
	PaneContent  string
	Permission   *provider.PermissionRequest
	CostUSD      float64
	TokensIn     int64
	TokensOut    int64
	ContextPct   int
	LastActivity string
	ChangedFiles []FileChange
	Events       []Event
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

type FileChange struct {
	Path   string
	Status string // "M", "A", "D"
}

type Event struct {
	Time    time.Time
	Type    string // "state_change", "permission", "tool_use", "error"
	Message string
}

type KPIData struct {
	ActiveCount  int
	TotalCount   int
	TotalCost    float64
	TotalTokens  int64
	LinesAdded   int
	LinesRemoved int
	Uptime       time.Duration
}
```

- [ ] **Step 2: Write tmux bridge test**

```go
package session

import (
	"os/exec"
	"testing"
)

func hasTmux() bool {
	_, err := exec.LookPath("tmux")
	return err == nil
}

func TestTmuxSessionName(t *testing.T) {
	tb := NewTmuxBridge("dv-")
	name := tb.SessionName("test-agent")
	if name != "dv-test-agent" {
		t.Errorf("expected 'dv-test-agent', got %q", name)
	}
}

func TestTmuxCreateAndKill(t *testing.T) {
	if !hasTmux() {
		t.Skip("tmux not available")
	}
	tb := NewTmuxBridge("dvtest-")

	err := tb.CreateSession("unit", "/tmp", "echo hello")
	if err != nil {
		t.Fatalf("CreateSession failed: %v", err)
	}

	sessions, err := tb.ListSessions()
	if err != nil {
		t.Fatalf("ListSessions failed: %v", err)
	}
	found := false
	for _, s := range sessions {
		if s == "dvtest-unit" {
			found = true
		}
	}
	if !found {
		t.Error("session 'dvtest-unit' not found in list")
	}

	err = tb.KillSession("unit")
	if err != nil {
		t.Fatalf("KillSession failed: %v", err)
	}
}
```

- [ ] **Step 3: Run test, verify it fails**

```bash
go test ./internal/session/ -v
```

- [ ] **Step 4: Implement tmux.go**

```go
package session

import (
	"fmt"
	"os/exec"
	"strings"
)

type TmuxBridge struct {
	prefix string
}

func NewTmuxBridge(prefix string) *TmuxBridge {
	return &TmuxBridge{prefix: prefix}
}

func (tb *TmuxBridge) SessionName(name string) string {
	return tb.prefix + name
}

func (tb *TmuxBridge) CreateSession(name, workDir, command string) error {
	sessionName := tb.SessionName(name)
	cmd := exec.Command("tmux", "new-session", "-d", "-s", sessionName, "-c", workDir, command)
	return cmd.Run()
}

func (tb *TmuxBridge) KillSession(name string) error {
	sessionName := tb.SessionName(name)
	cmd := exec.Command("tmux", "kill-session", "-t", sessionName)
	return cmd.Run()
}

func (tb *TmuxBridge) CapturePaneContent(name string, lines int) (string, error) {
	sessionName := tb.SessionName(name)
	cmd := exec.Command("tmux", "capture-pane", "-t", sessionName, "-p", "-S", fmt.Sprintf("-%d", lines))
	out, err := cmd.Output()
	if err != nil {
		return "", err
	}
	return string(out), nil
}

func (tb *TmuxBridge) SendKeys(name, keys string) error {
	sessionName := tb.SessionName(name)
	cmd := exec.Command("tmux", "send-keys", "-t", sessionName, keys, "Enter")
	return cmd.Run()
}

func (tb *TmuxBridge) SendRawKey(name, key string) error {
	sessionName := tb.SessionName(name)
	cmd := exec.Command("tmux", "send-keys", "-t", sessionName, key)
	return cmd.Run()
}

func (tb *TmuxBridge) ListSessions() ([]string, error) {
	cmd := exec.Command("tmux", "list-sessions", "-F", "#{session_name}")
	out, err := cmd.Output()
	if err != nil {
		// tmux returns error if no sessions exist
		if strings.Contains(err.Error(), "no server running") || strings.Contains(string(out), "no server") {
			return nil, nil
		}
		return nil, err
	}
	lines := strings.Split(strings.TrimSpace(string(out)), "\n")
	var sessions []string
	for _, line := range lines {
		line = strings.TrimSpace(line)
		if line != "" && strings.HasPrefix(line, tb.prefix) {
			sessions = append(sessions, line)
		}
	}
	return sessions, nil
}

func (tb *TmuxBridge) AttachCmd(name string) string {
	return fmt.Sprintf("tmux attach-session -t %s", tb.SessionName(name))
}
```

- [ ] **Step 5: Run tests**

```bash
go test ./internal/session/ -v
```

Expected: PASS (tmux test skipped if tmux not installed)

- [ ] **Step 6: Commit**

```bash
git add internal/session/
git commit -m "feat: session types and tmux bridge"
```

---

## Task 5: Session Manager

**Files:**
- Create: `internal/session/manager.go`
- Test: `internal/session/manager_test.go`

- [ ] **Step 1: Write manager test**

```go
package session

import (
	"testing"

	"github.com/meher/delvop/internal/config"
	"github.com/meher/delvop/internal/provider"
)

func TestManagerAddAndGet(t *testing.T) {
	cfg := config.Default()
	mgr := NewManager(cfg)

	p, _ := provider.Get("claude")
	sess, err := mgr.Add("test-agent", p, "opus", "/tmp", "")
	if err != nil {
		t.Fatalf("Add failed: %v", err)
	}
	if sess.Name != "test-agent" {
		t.Errorf("expected name 'test-agent', got %q", sess.Name)
	}
	if sess.ProviderName != "claude" {
		t.Errorf("expected provider 'claude', got %q", sess.ProviderName)
	}

	got, ok := mgr.Get(sess.ID)
	if !ok {
		t.Fatal("Get returned false")
	}
	if got.ID != sess.ID {
		t.Error("IDs don't match")
	}

	all := mgr.All()
	if len(all) != 1 {
		t.Errorf("expected 1 session, got %d", len(all))
	}
}

func TestManagerKPI(t *testing.T) {
	cfg := config.Default()
	mgr := NewManager(cfg)

	p, _ := provider.Get("claude")
	s, _ := mgr.Add("a1", p, "opus", "/tmp", "")
	s.State = provider.StateWorking
	s.CostUSD = 1.50

	s2, _ := mgr.Add("a2", p, "sonnet", "/tmp", "")
	s2.State = provider.StateWaitingForPermission
	s2.CostUSD = 0.75

	kpi := mgr.KPI()
	if kpi.ActiveCount != 2 {
		t.Errorf("expected 2 active, got %d", kpi.ActiveCount)
	}
	if kpi.TotalCost != 2.25 {
		t.Errorf("expected cost 2.25, got %f", kpi.TotalCost)
	}
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/session/ -v -run TestManager
```

- [ ] **Step 3: Implement manager.go**

```go
package session

import (
	"crypto/rand"
	"fmt"
	"strings"
	"sync"
	"time"

	"github.com/meher/delvop/internal/config"
	"github.com/meher/delvop/internal/provider"
)

type Manager struct {
	sessions map[string]*Session
	order    []string
	tmux     *TmuxBridge
	cfg      *config.Config
	mu       sync.RWMutex
}

func NewManager(cfg *config.Config) *Manager {
	return &Manager{
		sessions: make(map[string]*Session),
		tmux:     NewTmuxBridge(cfg.Tmux.Prefix),
		cfg:      cfg,
	}
}

func (m *Manager) Add(name string, p provider.AgentProvider, model, workDir, branch string) (*Session, error) {
	m.mu.Lock()
	defer m.mu.Unlock()

	id := shortID()
	sess := &Session{
		ID:           id,
		Name:         name,
		TmuxSession:  m.tmux.SessionName(name),
		Provider:     p,
		ProviderName: strings.ToLower(strings.Split(p.Name(), " ")[0]),
		Model:        model,
		WorkDir:      workDir,
		Branch:       branch,
		State:        provider.StateIdle,
		CreatedAt:    time.Now(),
		UpdatedAt:    time.Now(),
	}
	m.sessions[id] = sess
	m.order = append(m.order, id)
	return sess, nil
}

func (m *Manager) Launch(sess *Session) error {
	cmd := sess.Provider.LaunchCmd(sess.Model)
	return m.tmux.CreateSession(sess.Name, sess.WorkDir, cmd)
}

func (m *Manager) Get(id string) (*Session, bool) {
	m.mu.RLock()
	defer m.mu.RUnlock()
	s, ok := m.sessions[id]
	return s, ok
}

func (m *Manager) All() []*Session {
	m.mu.RLock()
	defer m.mu.RUnlock()
	result := make([]*Session, 0, len(m.order))
	for _, id := range m.order {
		if s, ok := m.sessions[id]; ok {
			result = append(result, s)
		}
	}
	return result
}

func (m *Manager) Remove(id string) {
	m.mu.Lock()
	defer m.mu.Unlock()
	if s, ok := m.sessions[id]; ok {
		_ = m.tmux.KillSession(s.Name)
		delete(m.sessions, id)
		for i, oid := range m.order {
			if oid == id {
				m.order = append(m.order[:i], m.order[i+1:]...)
				break
			}
		}
	}
}

func (m *Manager) PollState(id string) {
	m.mu.Lock()
	defer m.mu.Unlock()
	s, ok := m.sessions[id]
	if !ok {
		return
	}
	content, err := m.tmux.CapturePaneContent(s.Name, 30)
	if err != nil {
		return
	}
	s.PaneContent = content
	newState := s.Provider.ParseState(content)
	if newState != s.State {
		oldState := s.State
		s.State = newState
		s.UpdatedAt = time.Now()
		s.Events = append(s.Events, Event{
			Time:    time.Now(),
			Type:    "state_change",
			Message: fmt.Sprintf("%s → %s", oldState, newState),
		})
	}
	if newState == provider.StateWaitingForPermission {
		s.Permission = s.Provider.ParsePermission(content)
	} else {
		s.Permission = nil
	}
	cost, tokIn, tokOut := s.Provider.ParseCost(content)
	if cost > 0 {
		s.CostUSD = cost
		s.TokensIn = tokIn
		s.TokensOut = tokOut
	}
}

func (m *Manager) PollAll() {
	m.mu.RLock()
	ids := make([]string, len(m.order))
	copy(ids, m.order)
	m.mu.RUnlock()

	for _, id := range ids {
		m.PollState(id)
	}
}

func (m *Manager) SendKeys(id, keys string) error {
	m.mu.RLock()
	s, ok := m.sessions[id]
	m.mu.RUnlock()
	if !ok {
		return fmt.Errorf("session %s not found", id)
	}
	return m.tmux.SendRawKey(s.Name, keys)
}

func (m *Manager) Approve(id string) error {
	m.mu.RLock()
	s, ok := m.sessions[id]
	m.mu.RUnlock()
	if !ok {
		return fmt.Errorf("session %s not found", id)
	}
	return m.tmux.SendRawKey(s.Name, s.Provider.ApproveKey())
}

func (m *Manager) Deny(id string) error {
	m.mu.RLock()
	s, ok := m.sessions[id]
	m.mu.RUnlock()
	if !ok {
		return fmt.Errorf("session %s not found", id)
	}
	return m.tmux.SendRawKey(s.Name, s.Provider.DenyKey())
}

func (m *Manager) Compact(id string) error {
	m.mu.RLock()
	s, ok := m.sessions[id]
	m.mu.RUnlock()
	if !ok {
		return fmt.Errorf("session %s not found", id)
	}
	cmd := s.Provider.CompactCmd()
	if cmd == "" {
		return fmt.Errorf("provider %s does not support compacting", s.Provider.Name())
	}
	return m.tmux.SendKeys(s.Name, cmd)
}

func (m *Manager) AttachCmd(id string) string {
	m.mu.RLock()
	s, ok := m.sessions[id]
	m.mu.RUnlock()
	if !ok {
		return ""
	}
	return m.tmux.AttachCmd(s.Name)
}

func (m *Manager) NeedsAttention() []*Session {
	m.mu.RLock()
	defer m.mu.RUnlock()
	var result []*Session
	for _, id := range m.order {
		if s, ok := m.sessions[id]; ok && s.State == provider.StateWaitingForPermission {
			result = append(result, s)
		}
	}
	return result
}

func (m *Manager) KPI() KPIData {
	m.mu.RLock()
	defer m.mu.RUnlock()
	kpi := KPIData{TotalCount: len(m.sessions)}
	var earliest time.Time
	for _, s := range m.sessions {
		if s.State == provider.StateWorking || s.State == provider.StateWaitingForPermission || s.State == provider.StateIdle {
			kpi.ActiveCount++
		}
		kpi.TotalCost += s.CostUSD
		kpi.TotalTokens += s.TokensIn + s.TokensOut
		if earliest.IsZero() || s.CreatedAt.Before(earliest) {
			earliest = s.CreatedAt
		}
	}
	if !earliest.IsZero() {
		kpi.Uptime = time.Since(earliest)
	}
	return kpi
}

func (m *Manager) Tmux() *TmuxBridge {
	return m.tmux
}

func shortID() string {
	b := make([]byte, 4)
	rand.Read(b)
	return fmt.Sprintf("%x", b)
}
```

Note: Add `"strings"` to the import block in manager.go.

- [ ] **Step 4: Run tests**

```bash
go test ./internal/session/ -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/session/manager.go internal/session/manager_test.go
git commit -m "feat: session manager with lifecycle and state polling"
```

---

## Task 6: Hook Engine

**Files:**
- Create: `internal/hooks/engine.go`
- Test: `internal/hooks/engine_test.go`

- [ ] **Step 1: Write hook engine test**

```go
package hooks

import (
	"encoding/json"
	"net"
	"os"
	"path/filepath"
	"testing"
	"time"
)

func TestEngineStartStop(t *testing.T) {
	sockPath := filepath.Join(t.TempDir(), "test.sock")
	e := New(sockPath)

	err := e.Start()
	if err != nil {
		t.Fatalf("Start failed: %v", err)
	}

	// Verify socket file exists
	if _, err := os.Stat(sockPath); err != nil {
		t.Fatalf("socket file not created: %v", err)
	}

	e.Stop()

	// Socket should be cleaned up
	if _, err := os.Stat(sockPath); !os.IsNotExist(err) {
		t.Error("socket file not cleaned up")
	}
}

func TestEngineReceiveEvent(t *testing.T) {
	sockPath := filepath.Join(t.TempDir(), "test.sock")
	e := New(sockPath)

	err := e.Start()
	if err != nil {
		t.Fatal(err)
	}
	defer e.Stop()

	// Subscribe to events
	events := e.Subscribe()

	// Send a test event
	conn, err := net.Dial("unix", sockPath)
	if err != nil {
		t.Fatal(err)
	}
	evt := HookEvent{
		SessionID: "test-session",
		Type:      "state_change",
		Data:      map[string]interface{}{"state": "working"},
	}
	json.NewEncoder(conn).Encode(evt)
	conn.Close()

	// Should receive the event
	select {
	case received := <-events:
		if received.SessionID != "test-session" {
			t.Errorf("expected session 'test-session', got %q", received.SessionID)
		}
	case <-time.After(2 * time.Second):
		t.Error("timed out waiting for event")
	}
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/hooks/ -v
```

- [ ] **Step 3: Implement engine.go**

```go
package hooks

import (
	"encoding/json"
	"log"
	"net"
	"os"
	"sync"
)

type HookEvent struct {
	SessionID string                 `json:"session_id"`
	Type      string                 `json:"type"`
	Data      map[string]interface{} `json:"data"`
}

type Engine struct {
	socketPath  string
	listener    net.Listener
	subscribers []chan HookEvent
	mu          sync.RWMutex
	done        chan struct{}
}

func New(socketPath string) *Engine {
	return &Engine{
		socketPath: socketPath,
		done:       make(chan struct{}),
	}
}

func (e *Engine) Start() error {
	// Clean up stale socket
	os.Remove(e.socketPath)

	ln, err := net.Listen("unix", e.socketPath)
	if err != nil {
		return err
	}
	e.listener = ln

	go e.acceptLoop()
	return nil
}

func (e *Engine) Stop() {
	close(e.done)
	if e.listener != nil {
		e.listener.Close()
	}
	os.Remove(e.socketPath)
}

func (e *Engine) Subscribe() <-chan HookEvent {
	e.mu.Lock()
	defer e.mu.Unlock()
	ch := make(chan HookEvent, 64)
	e.subscribers = append(e.subscribers, ch)
	return ch
}

func (e *Engine) acceptLoop() {
	for {
		conn, err := e.listener.Accept()
		if err != nil {
			select {
			case <-e.done:
				return
			default:
				log.Printf("hook accept error: %v", err)
				continue
			}
		}
		go e.handleConn(conn)
	}
}

func (e *Engine) handleConn(conn net.Conn) {
	defer conn.Close()
	decoder := json.NewDecoder(conn)
	var evt HookEvent
	if err := decoder.Decode(&evt); err != nil {
		return
	}
	e.broadcast(evt)
}

func (e *Engine) broadcast(evt HookEvent) {
	e.mu.RLock()
	defer e.mu.RUnlock()
	for _, ch := range e.subscribers {
		select {
		case ch <- evt:
		default:
			// Drop if subscriber is full
		}
	}
}

func (e *Engine) SocketPath() string {
	return e.socketPath
}
```

- [ ] **Step 4: Run tests**

```bash
go test ./internal/hooks/ -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/hooks/
git commit -m "feat: Unix socket hook engine for agent events"
```

---

## Task 7: Notification Router

**Files:**
- Create: `internal/notify/router.go`
- Test: `internal/notify/router_test.go`

- [ ] **Step 1: Write notification test**

```go
package notify

import (
	"testing"
	"time"

	"github.com/meher/delvop/internal/config"
)

func TestDebounce(t *testing.T) {
	cfg := config.Default()
	cfg.Notify.Channels = []string{} // No actual notifications in test
	r := New(cfg)

	// Two events within debounce window — should be collapsed
	r.Notify("test-session", EventInputNeeded, "Agent needs input")
	r.Notify("test-session", EventInputNeeded, "Agent needs input again")

	// Check that only one notification was tracked
	count := r.RecentCount("test-session")
	if count > 1 {
		t.Errorf("expected debounce to collapse, got %d notifications", count)
	}
}

func TestFocusSuppression(t *testing.T) {
	cfg := config.Default()
	cfg.Notify.Channels = []string{}
	cfg.Notify.FocusSuppress = true
	r := New(cfg)
	r.SetFocused("test-session")

	r.Notify("test-session", EventInputNeeded, "Agent needs input")
	count := r.RecentCount("test-session")
	if count != 0 {
		t.Errorf("expected focus suppression, got %d notifications", count)
	}

	// Different session should not be suppressed
	r.Notify("other-session", EventInputNeeded, "Other agent")
	_ = time.Now() // just to use time
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/notify/ -v
```

- [ ] **Step 3: Implement router.go**

```go
package notify

import (
	"fmt"
	"os/exec"
	"runtime"
	"sync"
	"time"

	"github.com/meher/delvop/internal/config"
)

type EventType int

const (
	EventInputNeeded EventType = iota
	EventTaskDone
	EventError
)

type notification struct {
	sessionID string
	eventType EventType
	message   string
	time      time.Time
}

type Router struct {
	cfg     *config.Config
	focused string
	recent  []notification
	mu      sync.Mutex
}

func New(cfg *config.Config) *Router {
	return &Router{cfg: cfg}
}

func (r *Router) SetFocused(sessionID string) {
	r.mu.Lock()
	defer r.mu.Unlock()
	r.focused = sessionID
}

func (r *Router) Notify(sessionID string, eventType EventType, message string) {
	r.mu.Lock()
	defer r.mu.Unlock()

	// Focus suppression
	if r.cfg.Notify.FocusSuppress && sessionID == r.focused {
		return
	}

	// Debounce: check for recent notification from same session
	debounce := time.Duration(r.cfg.Notify.DebounceMs) * time.Millisecond
	for _, n := range r.recent {
		if n.sessionID == sessionID && n.eventType == eventType &&
			time.Since(n.time) < debounce {
			return
		}
	}

	n := notification{
		sessionID: sessionID,
		eventType: eventType,
		message:   message,
		time:      time.Now(),
	}
	r.recent = append(r.recent, n)

	// Prune old entries
	cutoff := time.Now().Add(-10 * time.Second)
	pruned := r.recent[:0]
	for _, n := range r.recent {
		if n.time.After(cutoff) {
			pruned = append(pruned, n)
		}
	}
	r.recent = pruned

	// Send through channels
	for _, ch := range r.cfg.Notify.Channels {
		switch ch {
		case "native":
			go r.sendNative(message)
		case "sound":
			go r.sendSound(eventType)
		}
	}
}

func (r *Router) RecentCount(sessionID string) int {
	r.mu.Lock()
	defer r.mu.Unlock()
	count := 0
	for _, n := range r.recent {
		if n.sessionID == sessionID {
			count++
		}
	}
	return count
}

func (r *Router) sendNative(message string) {
	if runtime.GOOS == "darwin" {
		script := fmt.Sprintf(`display notification "%s" with title "delvop" sound name "Basso"`, message)
		exec.Command("osascript", "-e", script).Run()
	} else {
		exec.Command("notify-send", "delvop", message).Run()
	}
}

func (r *Router) sendSound(eventType EventType) {
	if runtime.GOOS != "darwin" {
		return
	}
	sound := r.cfg.Notify.Sound.InputNeeded
	switch eventType {
	case EventTaskDone:
		sound = r.cfg.Notify.Sound.TaskDone
	case EventError:
		sound = r.cfg.Notify.Sound.Error
	}
	exec.Command("afplay", fmt.Sprintf("/System/Library/Sounds/%s.aiff", sound)).Run()
}
```

- [ ] **Step 4: Run tests**

```bash
go test ./internal/notify/ -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/notify/
git commit -m "feat: notification router with debounce and focus suppression"
```

---

## Task 8: Cost Tracker + SQLite Store

**Files:**
- Create: `internal/cost/tracker.go`, `internal/cost/store.go`
- Test: `internal/cost/tracker_test.go`

- [ ] **Step 1: Write cost tracker test**

```go
package cost

import (
	"path/filepath"
	"testing"
)

func TestTrackerUpdate(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "test.db")
	store, err := NewStore(dbPath)
	if err != nil {
		t.Fatal(err)
	}
	defer store.Close()

	tracker := NewTracker(store)
	tracker.Update("session-1", 1.50, 10000, 5000)
	tracker.Update("session-1", 2.00, 15000, 7000) // should replace

	entry, ok := tracker.Get("session-1")
	if !ok {
		t.Fatal("expected entry")
	}
	if entry.CostUSD != 2.00 {
		t.Errorf("expected cost 2.00, got %f", entry.CostUSD)
	}
	if entry.TokensIn != 15000 {
		t.Errorf("expected tokensIn 15000, got %d", entry.TokensIn)
	}

	total := tracker.TotalCost()
	if total != 2.00 {
		t.Errorf("expected total 2.00, got %f", total)
	}
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/cost/ -v
```

- [ ] **Step 3: Implement store.go**

```go
package cost

import (
	"database/sql"
	"time"

	_ "github.com/mattn/go-sqlite3"
)

type Store struct {
	db *sql.DB
}

func NewStore(dbPath string) (*Store, error) {
	db, err := sql.Open("sqlite3", dbPath)
	if err != nil {
		return nil, err
	}
	_, err = db.Exec(`CREATE TABLE IF NOT EXISTS cost_entries (
		session_id TEXT PRIMARY KEY,
		cost_usd REAL,
		tokens_in INTEGER,
		tokens_out INTEGER,
		updated_at DATETIME
	)`)
	if err != nil {
		db.Close()
		return nil, err
	}
	return &Store{db: db}, nil
}

func (s *Store) Upsert(sessionID string, costUSD float64, tokensIn, tokensOut int64) error {
	_, err := s.db.Exec(`INSERT INTO cost_entries (session_id, cost_usd, tokens_in, tokens_out, updated_at)
		VALUES (?, ?, ?, ?, ?)
		ON CONFLICT(session_id) DO UPDATE SET cost_usd=?, tokens_in=?, tokens_out=?, updated_at=?`,
		sessionID, costUSD, tokensIn, tokensOut, time.Now(),
		costUSD, tokensIn, tokensOut, time.Now())
	return err
}

func (s *Store) Close() error {
	return s.db.Close()
}
```

- [ ] **Step 4: Implement tracker.go**

```go
package cost

import (
	"sync"
)

type Entry struct {
	SessionID string
	CostUSD   float64
	TokensIn  int64
	TokensOut int64
}

type Tracker struct {
	entries map[string]*Entry
	store   *Store
	mu      sync.RWMutex
}

func NewTracker(store *Store) *Tracker {
	return &Tracker{
		entries: make(map[string]*Entry),
		store:   store,
	}
}

func (t *Tracker) Update(sessionID string, costUSD float64, tokensIn, tokensOut int64) {
	t.mu.Lock()
	defer t.mu.Unlock()
	t.entries[sessionID] = &Entry{
		SessionID: sessionID,
		CostUSD:   costUSD,
		TokensIn:  tokensIn,
		TokensOut: tokensOut,
	}
	if t.store != nil {
		t.store.Upsert(sessionID, costUSD, tokensIn, tokensOut)
	}
}

func (t *Tracker) Get(sessionID string) (*Entry, bool) {
	t.mu.RLock()
	defer t.mu.RUnlock()
	e, ok := t.entries[sessionID]
	return e, ok
}

func (t *Tracker) TotalCost() float64 {
	t.mu.RLock()
	defer t.mu.RUnlock()
	total := 0.0
	for _, e := range t.entries {
		total += e.CostUSD
	}
	return total
}
```

- [ ] **Step 5: Install sqlite3 dependency + run tests**

```bash
go get github.com/mattn/go-sqlite3@latest
go mod tidy
go test ./internal/cost/ -v
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add internal/cost/ go.mod go.sum
git commit -m "feat: cost tracker with SQLite persistence"
```

---

## Task 9: TUI Theme + Styles

**Files:**
- Create: `internal/tui/styles/theme.go`

- [ ] **Step 1: Create theme.go**

```go
package styles

import "github.com/charmbracelet/lipgloss"

// Tokyo Night color palette
var (
	Background    = lipgloss.Color("#0f0f14")
	Surface       = lipgloss.Color("#13131a")
	Border        = lipgloss.Color("#1e1e2e")
	TextPrimary   = lipgloss.Color("#e2e4f0")
	TextSecondary = lipgloss.Color("#a0a4b8")
	TextMuted     = lipgloss.Color("#787c99")
	TextDim       = lipgloss.Color("#4a4a5e")
	TextGhost     = lipgloss.Color("#3a3a4e")
	Purple        = lipgloss.Color("#8b7cf6")
	Green         = lipgloss.Color("#3dd68c")
	Red           = lipgloss.Color("#f06449")
	Blue          = lipgloss.Color("#4a8bf5")
	Amber         = lipgloss.Color("#e0af68")
)

// Semantic styles
var (
	StatusBar = lipgloss.NewStyle().
			Background(lipgloss.Color("#1a1a2e")).
			Foreground(TextSecondary).
			Padding(0, 1)

	KPIBar = lipgloss.NewStyle().
		Foreground(TextMuted).
		Padding(0, 1)

	AgentCard = lipgloss.NewStyle().
			Border(lipgloss.RoundedBorder()).
			BorderForeground(Border).
			Padding(0, 1).
			Width(20)

	AgentCardFocused = lipgloss.NewStyle().
				Border(lipgloss.RoundedBorder()).
				BorderForeground(Purple).
				Padding(0, 1).
				Width(20)

	ActivityFeed = lipgloss.NewStyle().
			Border(lipgloss.NormalBorder(), false, true, false, false).
			BorderForeground(Border).
			Padding(0, 1)

	ActionQueue = lipgloss.NewStyle().
			Padding(0, 1)

	HelpBar = lipgloss.NewStyle().
		Foreground(TextGhost).
		Padding(0, 1)
)

func StateColor(state string) lipgloss.Color {
	switch state {
	case "WORKING":
		return Green
	case "NEEDS INPUT":
		return Red
	case "DONE":
		return Blue
	case "IDLE":
		return TextMuted
	case "ERROR":
		return Red
	default:
		return TextDim
	}
}

func StateStyle(state string) lipgloss.Style {
	return lipgloss.NewStyle().
		Foreground(StateColor(state)).
		Bold(true)
}

func BrandStyle() lipgloss.Style {
	return lipgloss.NewStyle().
		Foreground(Purple).
		Bold(true)
}

func CostStyle() lipgloss.Style {
	return lipgloss.NewStyle().
		Foreground(Amber)
}

func KeyStyle() lipgloss.Style {
	return lipgloss.NewStyle().
		Foreground(Purple).
		Bold(true)
}

func DescStyle() lipgloss.Style {
	return lipgloss.NewStyle().
		Foreground(TextGhost)
}

func ProgressBar(pct int, width int) string {
	filled := width * pct / 100
	if filled > width {
		filled = width
	}
	bar := ""
	for i := 0; i < width; i++ {
		if i < filled {
			bar += "█"
		} else {
			bar += "░"
		}
	}
	return lipgloss.NewStyle().Foreground(Purple).Render(bar)
}
```

- [ ] **Step 2: Install lipgloss + verify compilation**

```bash
go get github.com/charmbracelet/lipgloss@latest
go build ./internal/tui/styles/
```

Expected: Compiles

- [ ] **Step 3: Commit**

```bash
git add internal/tui/styles/
git commit -m "feat: Tokyo Night theme and semantic styles"
```

---

## Task 10: TUI Messages + Key Bindings

**Files:**
- Create: `internal/tui/messages.go`, `internal/tui/keys.go`

- [ ] **Step 1: Create messages.go**

```go
package tui

import (
	"github.com/meher/delvop/internal/hooks"
)

// TickMsg fires every poll interval to refresh state
type TickMsg struct{}

// HookEventMsg wraps a hook event from the Unix socket
type HookEventMsg struct {
	Event hooks.HookEvent
}

// SessionCreatedMsg indicates a new session was added
type SessionCreatedMsg struct {
	SessionID string
}

// SessionKilledMsg indicates a session was removed
type SessionKilledMsg struct {
	SessionID string
}

// ErrorMsg carries an error to display in the status bar
type ErrorMsg struct {
	Err error
}

// StatusMsg carries a temporary status message
type StatusMsg struct {
	Message string
}
```

- [ ] **Step 2: Create keys.go**

```go
package tui

import "github.com/charmbracelet/bubbles/key"

type KeyMap struct {
	Up      key.Binding
	Down    key.Binding
	Enter   key.Binding
	Escape  key.Binding
	Tab     key.Binding
	ShiftTab key.Binding
	New     key.Binding
	Template key.Binding
	Approve key.Binding
	Deny    key.Binding
	Message key.Binding
	Kill    key.Binding
	Compact key.Binding
	Quit    key.Binding
	Help    key.Binding
}

var Keys = KeyMap{
	Up: key.NewBinding(
		key.WithKeys("up", "k"),
		key.WithHelp("↑/k", "up"),
	),
	Down: key.NewBinding(
		key.WithKeys("down", "j"),
		key.WithHelp("↓/j", "down"),
	),
	Enter: key.NewBinding(
		key.WithKeys("enter"),
		key.WithHelp("enter", "focus/zoom"),
	),
	Escape: key.NewBinding(
		key.WithKeys("esc"),
		key.WithHelp("esc", "back"),
	),
	Tab: key.NewBinding(
		key.WithKeys("tab"),
		key.WithHelp("tab", "next panel"),
	),
	ShiftTab: key.NewBinding(
		key.WithKeys("shift+tab"),
		key.WithHelp("shift+tab", "prev panel"),
	),
	New: key.NewBinding(
		key.WithKeys("n"),
		key.WithHelp("n", "new agent"),
	),
	Template: key.NewBinding(
		key.WithKeys("t"),
		key.WithHelp("t", "from template"),
	),
	Approve: key.NewBinding(
		key.WithKeys("y"),
		key.WithHelp("y", "approve"),
	),
	Deny: key.NewBinding(
		key.WithKeys("N"),
		key.WithHelp("N", "deny"),
	),
	Message: key.NewBinding(
		key.WithKeys("m"),
		key.WithHelp("m", "message"),
	),
	Kill: key.NewBinding(
		key.WithKeys("x"),
		key.WithHelp("x", "kill agent"),
	),
	Compact: key.NewBinding(
		key.WithKeys("c"),
		key.WithHelp("c", "compact"),
	),
	Quit: key.NewBinding(
		key.WithKeys("q", "ctrl+c"),
		key.WithHelp("q", "quit"),
	),
	Help: key.NewBinding(
		key.WithKeys("?"),
		key.WithHelp("?", "help"),
	),
}
```

- [ ] **Step 3: Install bubbles dependency + verify compilation**

```bash
go get github.com/charmbracelet/bubbles@latest
go build ./internal/tui/
```

Expected: Compiles (may have errors until model.go exists — that's fine, we're building incrementally)

- [ ] **Step 4: Commit**

```bash
git add internal/tui/messages.go internal/tui/keys.go
git commit -m "feat: TUI message types and key bindings"
```

---

## Task 11: TUI Model + Update + View (Core Dashboard)

This is the largest task — the main TUI. We build all three files together since they're tightly coupled.

**Files:**
- Create: `internal/tui/model.go`, `internal/tui/update.go`, `internal/tui/view.go`

- [ ] **Step 1: Create model.go**

```go
package tui

import (
	"time"

	"github.com/charmbracelet/bubbles/textinput"
	tea "github.com/charmbracelet/bubbletea"

	"github.com/meher/delvop/internal/config"
	"github.com/meher/delvop/internal/hooks"
	"github.com/meher/delvop/internal/notify"
	"github.com/meher/delvop/internal/session"
)

type ViewMode int

const (
	ViewDashboard ViewMode = iota
	ViewFocused
)

type Model struct {
	cfg      *config.Config
	manager  *session.Manager
	hooks    *hooks.Engine
	notifier *notify.Router

	viewMode     ViewMode
	selectedIdx  int
	width        int
	height       int
	statusMsg    string
	statusExpiry time.Time
	showHelp     bool

	// Input mode
	inputMode    bool
	inputPurpose string // "new_agent", "message"
	textInput    textinput.Model

	// Focused view
	focusedID    string
	scrollOffset int
}

func NewModel(cfg *config.Config, mgr *session.Manager, hookEngine *hooks.Engine, notif *notify.Router) Model {
	ti := textinput.New()
	ti.Placeholder = "Enter agent name..."
	ti.CharLimit = 64

	return Model{
		cfg:      cfg,
		manager:  mgr,
		hooks:    hookEngine,
		notifier: notif,
		textInput: ti,
	}
}

func (m Model) Init() tea.Cmd {
	return tea.Batch(
		tickCmd(m.cfg.General.PollIntervalMs),
		listenForHookEvents(m.hooks),
	)
}

func tickCmd(intervalMs int) tea.Cmd {
	return tea.Tick(time.Duration(intervalMs)*time.Millisecond, func(t time.Time) tea.Msg {
		return TickMsg{}
	})
}

func listenForHookEvents(engine *hooks.Engine) tea.Cmd {
	if engine == nil {
		return nil
	}
	ch := engine.Subscribe()
	return func() tea.Msg {
		evt := <-ch
		return HookEventMsg{Event: evt}
	}
}
```

- [ ] **Step 2: Create update.go**

```go
package tui

import (
	"fmt"
	"os/exec"
	"time"

	"github.com/charmbracelet/bubbles/key"
	tea "github.com/charmbracelet/bubbletea"

	"github.com/meher/delvop/internal/notify"
	"github.com/meher/delvop/internal/provider"
)

func (m Model) Update(msg tea.Msg) (tea.Model, tea.Cmd) {
	switch msg := msg.(type) {
	case tea.WindowSizeMsg:
		m.width = msg.Width
		m.height = msg.Height
		return m, nil

	case TickMsg:
		m.manager.PollAll()
		// Check for notifications
		for _, s := range m.manager.NeedsAttention() {
			m.notifier.Notify(s.ID, notify.EventInputNeeded,
				fmt.Sprintf("%s needs permission", s.Name))
		}
		return m, tickCmd(m.cfg.General.PollIntervalMs)

	case HookEventMsg:
		// Process hook event — re-subscribe
		return m, listenForHookEvents(m.hooks)

	case ErrorMsg:
		m.statusMsg = fmt.Sprintf("Error: %v", msg.Err)
		m.statusExpiry = time.Now().Add(5 * time.Second)
		return m, nil

	case StatusMsg:
		m.statusMsg = msg.Message
		m.statusExpiry = time.Now().Add(3 * time.Second)
		return m, nil

	case SessionCreatedMsg:
		m.statusMsg = "Agent created"
		m.statusExpiry = time.Now().Add(3 * time.Second)
		return m, nil

	case tea.KeyMsg:
		// Input mode handling
		if m.inputMode {
			return m.handleInputKey(msg)
		}

		switch m.viewMode {
		case ViewDashboard:
			return m.handleDashboardKey(msg)
		case ViewFocused:
			return m.handleFocusedKey(msg)
		}
	}

	return m, nil
}

func (m Model) handleDashboardKey(msg tea.KeyMsg) (tea.Model, tea.Cmd) {
	sessions := m.manager.All()

	switch {
	case key.Matches(msg, Keys.Quit):
		return m, tea.Quit

	case key.Matches(msg, Keys.Help):
		m.showHelp = !m.showHelp
		return m, nil

	case key.Matches(msg, Keys.Up):
		if m.selectedIdx > 0 {
			m.selectedIdx--
		}
		return m, nil

	case key.Matches(msg, Keys.Down):
		if m.selectedIdx < len(sessions)-1 {
			m.selectedIdx++
		}
		return m, nil

	case key.Matches(msg, Keys.Enter):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			m.focusedID = sessions[m.selectedIdx].ID
			m.viewMode = ViewFocused
			m.notifier.SetFocused(m.focusedID)
		}
		return m, nil

	case key.Matches(msg, Keys.New):
		m.inputMode = true
		m.inputPurpose = "new_agent"
		m.textInput.Placeholder = "Agent name (e.g. frontend, api-server)..."
		m.textInput.SetValue("")
		m.textInput.Focus()
		return m, m.textInput.Cursor.BlinkCmd()

	case key.Matches(msg, Keys.Approve):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			s := sessions[m.selectedIdx]
			if s.State == provider.StateWaitingForPermission {
				m.manager.Approve(s.ID)
				m.statusMsg = fmt.Sprintf("Approved %s", s.Name)
				m.statusExpiry = time.Now().Add(2 * time.Second)
			}
		}
		return m, nil

	case key.Matches(msg, Keys.Deny):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			s := sessions[m.selectedIdx]
			if s.State == provider.StateWaitingForPermission {
				m.manager.Deny(s.ID)
				m.statusMsg = fmt.Sprintf("Denied %s", s.Name)
				m.statusExpiry = time.Now().Add(2 * time.Second)
			}
		}
		return m, nil

	case key.Matches(msg, Keys.Message):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			m.inputMode = true
			m.inputPurpose = "message"
			m.textInput.Placeholder = "Message to send..."
			m.textInput.SetValue("")
			m.textInput.Focus()
			return m, m.textInput.Cursor.BlinkCmd()
		}
		return m, nil

	case key.Matches(msg, Keys.Kill):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			s := sessions[m.selectedIdx]
			m.manager.Remove(s.ID)
			if m.selectedIdx >= len(m.manager.All()) && m.selectedIdx > 0 {
				m.selectedIdx--
			}
			m.statusMsg = fmt.Sprintf("Killed %s", s.Name)
			m.statusExpiry = time.Now().Add(2 * time.Second)
		}
		return m, nil

	case key.Matches(msg, Keys.Compact):
		if len(sessions) > 0 && m.selectedIdx < len(sessions) {
			s := sessions[m.selectedIdx]
			if err := m.manager.Compact(s.ID); err != nil {
				m.statusMsg = fmt.Sprintf("Compact error: %v", err)
			} else {
				m.statusMsg = fmt.Sprintf("Compacting %s", s.Name)
			}
			m.statusExpiry = time.Now().Add(2 * time.Second)
		}
		return m, nil
	}

	return m, nil
}

func (m Model) handleFocusedKey(msg tea.KeyMsg) (tea.Model, tea.Cmd) {
	switch {
	case key.Matches(msg, Keys.Escape):
		m.viewMode = ViewDashboard
		m.notifier.SetFocused("")
		return m, nil

	case key.Matches(msg, Keys.Enter):
		// Drop into raw tmux terminal
		attachCmd := m.manager.AttachCmd(m.focusedID)
		if attachCmd != "" {
			c := exec.Command("sh", "-c", attachCmd)
			return m, tea.ExecProcess(c, func(err error) tea.Msg {
				if err != nil {
					return ErrorMsg{Err: err}
				}
				return StatusMsg{Message: "Detached from terminal"}
			})
		}
		return m, nil

	case key.Matches(msg, Keys.Up):
		if m.scrollOffset > 0 {
			m.scrollOffset--
		}
		return m, nil

	case key.Matches(msg, Keys.Down):
		m.scrollOffset++
		return m, nil

	case key.Matches(msg, Keys.Approve):
		m.manager.Approve(m.focusedID)
		return m, nil

	case key.Matches(msg, Keys.Deny):
		m.manager.Deny(m.focusedID)
		return m, nil

	case key.Matches(msg, Keys.Message):
		m.inputMode = true
		m.inputPurpose = "message"
		m.textInput.Placeholder = "Message to send..."
		m.textInput.SetValue("")
		m.textInput.Focus()
		return m, m.textInput.Cursor.BlinkCmd()

	case key.Matches(msg, Keys.Kill):
		m.manager.Remove(m.focusedID)
		m.viewMode = ViewDashboard
		return m, nil

	case key.Matches(msg, Keys.Tab):
		sessions := m.manager.All()
		for i, s := range sessions {
			if s.ID == m.focusedID {
				next := (i + 1) % len(sessions)
				m.focusedID = sessions[next].ID
				m.scrollOffset = 0
				break
			}
		}
		return m, nil

	case key.Matches(msg, Keys.Quit):
		return m, tea.Quit
	}

	return m, nil
}

func (m Model) handleInputKey(msg tea.KeyMsg) (tea.Model, tea.Cmd) {
	switch msg.String() {
	case "enter":
		value := m.textInput.Value()
		m.inputMode = false
		m.textInput.Blur()

		if value == "" {
			return m, nil
		}

		switch m.inputPurpose {
		case "new_agent":
			return m, m.createAgent(value)
		case "message":
			sessions := m.manager.All()
			var targetID string
			if m.viewMode == ViewFocused {
				targetID = m.focusedID
			} else if m.selectedIdx < len(sessions) {
				targetID = sessions[m.selectedIdx].ID
			}
			if targetID != "" {
				m.manager.SendKeys(targetID, value)
				m.statusMsg = fmt.Sprintf("Sent message to agent")
				m.statusExpiry = time.Now().Add(2 * time.Second)
			}
		}
		return m, nil

	case "esc":
		m.inputMode = false
		m.textInput.Blur()
		return m, nil
	}

	var cmd tea.Cmd
	m.textInput, cmd = m.textInput.Update(msg)
	return m, cmd
}

func (m Model) createAgent(name string) tea.Cmd {
	return func() tea.Msg {
		p, err := provider.Get(m.cfg.General.DefaultProvider)
		if err != nil {
			return ErrorMsg{Err: err}
		}

		sess, err := m.manager.Add(name, p, m.cfg.General.DefaultModel, ".", "")
		if err != nil {
			return ErrorMsg{Err: err}
		}

		if err := m.manager.Launch(sess); err != nil {
			return ErrorMsg{Err: fmt.Errorf("launch failed: %w (is %s installed?)", err, p.Name())}
		}

		return SessionCreatedMsg{SessionID: sess.ID}
	}
}
```

- [ ] **Step 3: Create view.go**

```go
package tui

import (
	"fmt"
	"strings"
	"time"

	"github.com/charmbracelet/lipgloss"

	"github.com/meher/delvop/internal/provider"
	"github.com/meher/delvop/internal/session"
	"github.com/meher/delvop/internal/tui/styles"
)

func (m Model) View() string {
	if m.width == 0 {
		return "Loading..."
	}

	switch m.viewMode {
	case ViewFocused:
		return m.viewFocused()
	default:
		return m.viewDashboard()
	}
}

func (m Model) viewDashboard() string {
	sessions := m.manager.All()
	kpi := m.manager.KPI()
	w := m.width

	var sections []string

	// Status bar
	statusBar := m.renderStatusBar(len(sessions))
	sections = append(sections, statusBar)

	// KPI bar
	kpiBar := m.renderKPIBar(kpi)
	sections = append(sections, kpiBar)

	// Agent cards or empty state
	if len(sessions) == 0 {
		sections = append(sections, m.renderEmptyState())
	} else {
		sections = append(sections, m.renderAgentGrid(sessions))
	}

	// Bottom section: activity + action queue side by side
	if len(sessions) > 0 {
		bottomHeight := m.height - lipgloss.Height(strings.Join(sections, "\n")) - 3
		if bottomHeight < 5 {
			bottomHeight = 5
		}
		leftWidth := w/2 - 1
		rightWidth := w - leftWidth - 1

		activityFeed := m.renderActivityFeed(sessions, leftWidth, bottomHeight)
		actionQueue := m.renderActionQueue(sessions, rightWidth, bottomHeight)

		bottom := lipgloss.JoinHorizontal(lipgloss.Top, activityFeed, actionQueue)
		sections = append(sections, bottom)
	}

	// Input overlay
	if m.inputMode {
		sections = append(sections, m.renderInput())
	}

	// Help bar
	sections = append(sections, m.renderHelpBar())

	// Help overlay
	if m.showHelp {
		return m.renderHelpOverlay()
	}

	return lipgloss.JoinVertical(lipgloss.Left, sections...)
}

func (m Model) renderStatusBar(agentCount int) string {
	needsAttention := len(m.manager.NeedsAttention())
	attention := ""
	if needsAttention > 0 {
		attention = lipgloss.NewStyle().Foreground(styles.Red).
			Render(fmt.Sprintf(" · %d needs you", needsAttention))
	}

	status := ""
	if m.statusMsg != "" && time.Now().Before(m.statusExpiry) {
		status = " · " + lipgloss.NewStyle().Foreground(styles.Amber).Render(m.statusMsg)
	}

	left := fmt.Sprintf(" %s · %d agents%s%s",
		styles.BrandStyle().Render("delvop"),
		agentCount, attention, status)
	right := "ctrl+p · ?:help "

	gap := m.width - lipgloss.Width(left) - lipgloss.Width(right)
	if gap < 0 {
		gap = 0
	}

	return lipgloss.NewStyle().
		Background(lipgloss.Color("#1a1a2e")).
		Width(m.width).
		Render(left + strings.Repeat(" ", gap) + lipgloss.NewStyle().Foreground(styles.TextGhost).Render(right))
}

func (m Model) renderKPIBar(kpi session.KPIData) string {
	items := []string{
		fmt.Sprintf("%d/%d active", kpi.ActiveCount, kpi.TotalCount),
		styles.CostStyle().Render(fmt.Sprintf("$%.2f cost", kpi.TotalCost)),
		fmt.Sprintf("%dk tokens", kpi.TotalTokens/1000),
		fmt.Sprintf("%s uptime", formatDuration(kpi.Uptime)),
	}
	return styles.KPIBar.Width(m.width).Render(strings.Join(items, " │ "))
}

func (m Model) renderAgentGrid(sessions []*session.Session) string {
	if len(sessions) == 0 {
		return ""
	}

	cardWidth := 22
	if m.width > 0 {
		maxCards := m.width / (cardWidth + 2)
		if maxCards < 1 {
			maxCards = 1
		}
		if maxCards > 4 {
			maxCards = 4
		}
		cardWidth = (m.width / maxCards) - 3
		if cardWidth < 18 {
			cardWidth = 18
		}
	}

	var cards []string
	for i, s := range sessions {
		cards = append(cards, m.renderAgentCard(s, i == m.selectedIdx, cardWidth))
	}

	// Arrange in rows of 4
	var rows []string
	for i := 0; i < len(cards); i += 4 {
		end := i + 4
		if end > len(cards) {
			end = len(cards)
		}
		rows = append(rows, lipgloss.JoinHorizontal(lipgloss.Top, cards[i:end]...))
	}

	return lipgloss.JoinVertical(lipgloss.Left, rows...)
}

func (m Model) renderAgentCard(s *session.Session, selected bool, width int) string {
	style := styles.AgentCard.Width(width)
	if selected {
		style = styles.AgentCardFocused.Width(width)
	}

	stateStr := s.State.String()
	stateLine := styles.StateStyle(stateStr).Render(stateStr)

	modelStr := s.Model
	if len(modelStr) > 8 {
		modelStr = modelStr[:8]
	}
	infoLine := lipgloss.NewStyle().Foreground(styles.TextMuted).
		Render(fmt.Sprintf("%s · %s", modelStr, s.Branch))

	costLine := styles.CostStyle().
		Render(fmt.Sprintf("$%.2f", s.CostUSD))

	// Last activity
	activity := lipgloss.NewStyle().Foreground(styles.TextDim).
		Render(truncate(s.LastActivity, width-4))

	// Changed files
	var files []string
	for _, f := range s.ChangedFiles {
		if len(files) >= 2 {
			break
		}
		marker := lipgloss.NewStyle().Foreground(styles.Green).Render(f.Status)
		files = append(files, fmt.Sprintf("%s %s", marker, truncate(f.Path, width-6)))
	}
	fileStr := lipgloss.NewStyle().Foreground(styles.TextDim).
		Render(strings.Join(files, "\n"))

	content := lipgloss.JoinVertical(lipgloss.Left,
		stateLine,
		infoLine,
		costLine,
		activity,
		fileStr,
	)

	header := lipgloss.NewStyle().Foreground(styles.TextPrimary).Bold(true).
		Render(truncate(s.Name, width-2))

	return style.Render(header + "\n" + content)
}

func (m Model) renderEmptyState() string {
	art := `
     ╭──────────────────────╮
     │      d e l v o p      │
     │                       │
     │   No agents running   │
     │                       │
     │   Press 'n' to hire   │
     │   your first agent    │
     ╰──────────────────────╯`

	return lipgloss.NewStyle().
		Foreground(styles.Purple).
		Align(lipgloss.Center).
		Width(m.width).
		Padding(2, 0).
		Render(art)
}

func (m Model) renderActivityFeed(sessions []*session.Session, width, height int) string {
	var lines []string
	// Collect all events, most recent first
	for _, s := range sessions {
		for i := len(s.Events) - 1; i >= 0; i-- {
			e := s.Events[i]
			ago := formatAgo(e.Time)
			line := fmt.Sprintf("%s  %s %s",
				lipgloss.NewStyle().Foreground(styles.TextDim).Render(ago),
				lipgloss.NewStyle().Foreground(styles.Purple).Render("●"),
				lipgloss.NewStyle().Foreground(styles.TextSecondary).
					Render(fmt.Sprintf("%s %s", s.Name, e.Message)))
			lines = append(lines, line)
		}
	}
	if len(lines) == 0 {
		lines = append(lines, lipgloss.NewStyle().Foreground(styles.TextDim).Render("  No activity yet"))
	}
	if len(lines) > height-2 {
		lines = lines[:height-2]
	}

	title := lipgloss.NewStyle().Foreground(styles.TextMuted).Bold(true).Render("Activity")
	content := title + "\n" + strings.Join(lines, "\n")

	return styles.ActivityFeed.Width(width).Height(height).Render(content)
}

func (m Model) renderActionQueue(sessions []*session.Session, width, height int) string {
	var lines []string
	for _, s := range sessions {
		if s.State == provider.StateWaitingForPermission && s.Permission != nil {
			header := lipgloss.NewStyle().Foreground(styles.Red).Bold(true).
				Render(fmt.Sprintf("%s → %s %s", s.Name, s.Permission.Tool, s.Permission.File))
			lines = append(lines, header)
			if s.Permission.Summary != "" {
				lines = append(lines, lipgloss.NewStyle().Foreground(styles.TextDim).
					Render(truncate(s.Permission.Summary, width-4)))
			}
			actions := fmt.Sprintf("[%s] allow  [%s] deny",
				styles.KeyStyle().Render("y"),
				styles.KeyStyle().Render("N"))
			lines = append(lines, actions)
			lines = append(lines, "")
		}
	}
	if len(lines) == 0 {
		lines = append(lines, lipgloss.NewStyle().Foreground(styles.TextDim).Render("  No pending actions"))
	}

	quickActions := fmt.Sprintf("Quick: [%s]ew [%s]sg [%s]ompact",
		styles.KeyStyle().Render("n"),
		styles.KeyStyle().Render("m"),
		styles.KeyStyle().Render("c"))
	lines = append(lines, "", quickActions)

	title := lipgloss.NewStyle().Foreground(styles.TextMuted).Bold(true).Render("Action Queue")
	content := title + "\n" + strings.Join(lines, "\n")

	return styles.ActionQueue.Width(width).Height(height).Render(content)
}

func (m Model) renderInput() string {
	return lipgloss.NewStyle().
		Border(lipgloss.RoundedBorder()).
		BorderForeground(styles.Purple).
		Padding(0, 1).
		Width(m.width - 4).
		Render(m.textInput.View())
}

func (m Model) renderHelpBar() string {
	keys := []struct{ key, desc string }{
		{"j/k", "select"},
		{"enter", "focus"},
		{"y/N", "respond"},
		{"n", "new"},
		{"m", "msg"},
		{"x", "kill"},
		{"?", "help"},
		{"q", "quit"},
	}
	var items []string
	for _, k := range keys {
		items = append(items,
			styles.KeyStyle().Render(k.key)+
				styles.DescStyle().Render(":"+k.desc))
	}
	return lipgloss.NewStyle().
		Width(m.width).
		Background(lipgloss.Color("#1a1a2e")).
		Render(" " + strings.Join(items, " · ") + " delvop v0.1")
}

func (m Model) renderHelpOverlay() string {
	help := `
  DASHBOARD SHORTCUTS

  Navigation
    j/k, ↑/↓    Navigate agents
    tab          Switch panels
    enter        Focus agent
    esc          Back

  Agent Management
    n            New agent
    t            From template
    m            Message agent
    c            Compact context
    x            Kill agent

  Permissions
    y            Approve
    N            Deny

  Global
    ?            Toggle help
    q            Quit (agents keep running)
    ctrl+c       Force quit

  FOCUSED VIEW
    enter        Drop into raw terminal
    tab          Next agent
    ctrl+\       Detach from raw terminal
`
	return lipgloss.NewStyle().
		Border(lipgloss.RoundedBorder()).
		BorderForeground(styles.Purple).
		Foreground(styles.TextSecondary).
		Padding(1, 2).
		Align(lipgloss.Center).
		Width(m.width - 10).
		Render(help)
}

func (m Model) viewFocused() string {
	s, ok := m.manager.Get(m.focusedID)
	if !ok {
		m.viewMode = ViewDashboard
		return m.viewDashboard()
	}

	var sections []string

	// Breadcrumb
	breadcrumb := fmt.Sprintf(" %s > %s",
		styles.BrandStyle().Render("delvop"),
		lipgloss.NewStyle().Foreground(styles.TextPrimary).Bold(true).Render(s.Name))
	sections = append(sections, lipgloss.NewStyle().
		Background(lipgloss.Color("#1a1a2e")).
		Width(m.width).Render(breadcrumb))

	// Agent profile
	stateStr := s.State.String()
	profile := fmt.Sprintf(
		"  %s  %s · %s · %s\n  %s  %s",
		styles.StateStyle(stateStr).Render(stateStr),
		lipgloss.NewStyle().Foreground(styles.TextMuted).Render(s.ProviderName),
		lipgloss.NewStyle().Foreground(styles.TextMuted).Render(s.Model),
		lipgloss.NewStyle().Foreground(styles.TextDim).Render(s.WorkDir),
		styles.CostStyle().Render(fmt.Sprintf("$%.2f", s.CostUSD)),
		lipgloss.NewStyle().Foreground(styles.TextDim).
			Render(fmt.Sprintf("%dk tokens", (s.TokensIn+s.TokensOut)/1000)),
	)
	sections = append(sections, profile)

	// Permission detail if any
	if s.State == provider.StateWaitingForPermission && s.Permission != nil {
		permBox := lipgloss.NewStyle().
			Border(lipgloss.RoundedBorder()).
			BorderForeground(styles.Red).
			Padding(0, 1).
			Width(m.width - 6).
			Render(fmt.Sprintf("Permission: %s %s\n%s\n\n[y] approve  [N] deny",
				s.Permission.Tool, s.Permission.File, s.Permission.Summary))
		sections = append(sections, permBox)
	}

	// Work log
	logTitle := lipgloss.NewStyle().Foreground(styles.TextMuted).Bold(true).
		Render("  Work Log")
	sections = append(sections, logTitle)

	maxEvents := m.height - lipgloss.Height(strings.Join(sections, "\n")) - 4
	if maxEvents < 3 {
		maxEvents = 3
	}
	events := s.Events
	start := m.scrollOffset
	if start > len(events) {
		start = len(events)
	}
	end := start + maxEvents
	if end > len(events) {
		end = len(events)
	}

	for _, e := range events[start:end] {
		line := fmt.Sprintf("  %s  %s  %s",
			lipgloss.NewStyle().Foreground(styles.TextDim).Render(e.Time.Format("15:04:05")),
			lipgloss.NewStyle().Foreground(styles.Purple).Render("●"),
			lipgloss.NewStyle().Foreground(styles.TextSecondary).Render(e.Message))
		sections = append(sections, line)
	}
	if len(events) == 0 {
		sections = append(sections, lipgloss.NewStyle().Foreground(styles.TextDim).
			Render("  No events yet"))
	}

	// Help bar for focused view
	focusedHelp := fmt.Sprintf(" %s:terminal · %s:back · %s:next · %s/%s:respond · %s:msg · %s:kill",
		styles.KeyStyle().Render("enter"),
		styles.KeyStyle().Render("esc"),
		styles.KeyStyle().Render("tab"),
		styles.KeyStyle().Render("y"),
		styles.KeyStyle().Render("N"),
		styles.KeyStyle().Render("m"),
		styles.KeyStyle().Render("x"))
	sections = append(sections, lipgloss.NewStyle().
		Width(m.width).
		Background(lipgloss.Color("#1a1a2e")).
		Foreground(styles.TextGhost).
		Render(focusedHelp))

	return lipgloss.JoinVertical(lipgloss.Left, sections...)
}

// Helpers

func truncate(s string, maxLen int) string {
	if maxLen <= 0 {
		return ""
	}
	if len(s) <= maxLen {
		return s
	}
	if maxLen <= 3 {
		return s[:maxLen]
	}
	return s[:maxLen-3] + "..."
}

func formatDuration(d time.Duration) string {
	if d < time.Minute {
		return fmt.Sprintf("%ds", int(d.Seconds()))
	}
	if d < time.Hour {
		return fmt.Sprintf("%dm", int(d.Minutes()))
	}
	return fmt.Sprintf("%dh%dm", int(d.Hours()), int(d.Minutes())%60)
}

func formatAgo(t time.Time) string {
	d := time.Since(t)
	if d < time.Minute {
		return "now"
	}
	if d < time.Hour {
		return fmt.Sprintf("%dm", int(d.Minutes()))
	}
	return fmt.Sprintf("%dh", int(d.Hours()))
}
```

- [ ] **Step 4: Install bubbletea dependency + verify compilation**

```bash
go get github.com/charmbracelet/bubbletea@latest
go mod tidy
go build ./internal/tui/
```

Expected: Compiles successfully

- [ ] **Step 5: Commit**

```bash
git add internal/tui/model.go internal/tui/update.go internal/tui/view.go
git commit -m "feat: core TUI with dashboard and focused views"
```

---

## Task 12: Wire CLI to TUI

**Files:**
- Modify: `cmd/delvop/root.go`

- [ ] **Step 1: Update root.go to wire everything together**

```go
package delvop

import (
	"fmt"
	"os"

	tea "github.com/charmbracelet/bubbletea"
	"github.com/spf13/cobra"

	"github.com/meher/delvop/internal/config"
	"github.com/meher/delvop/internal/hooks"
	"github.com/meher/delvop/internal/notify"
	"github.com/meher/delvop/internal/session"
	"github.com/meher/delvop/internal/tui"

	// Register providers
	_ "github.com/meher/delvop/internal/provider"
)

var rootCmd = &cobra.Command{
	Use:   "delvop",
	Short: "Engineering Command Center for Terminal Coding Agents",
	Long:  "Manage a team of AI coding agents from a single terminal dashboard.",
	RunE:  runDashboard,
}

func Execute() error {
	return rootCmd.Execute()
}

func runDashboard(cmd *cobra.Command, args []string) error {
	cfg, err := config.Load()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Warning: config load error: %v, using defaults\n", err)
		cfg = config.Default()
	}

	// Initialize hook engine
	hookEngine := hooks.New("/tmp/delvop.sock")
	if err := hookEngine.Start(); err != nil {
		fmt.Fprintf(os.Stderr, "Warning: hook engine start error: %v\n", err)
	}
	defer hookEngine.Stop()

	// Initialize notification router
	notifier := notify.New(cfg)

	// Initialize session manager
	mgr := session.NewManager(cfg)

	// Create and run TUI
	model := tui.NewModel(cfg, mgr, hookEngine, notifier)
	p := tea.NewProgram(model, tea.WithAltScreen())

	_, err = p.Run()
	return err
}
```

- [ ] **Step 2: Verify full build compiles**

```bash
go mod tidy
go build .
```

Expected: Binary `delvop` builds successfully

- [ ] **Step 3: Run the binary**

```bash
./delvop
```

Expected: TUI launches with empty state splash screen. Press `q` to quit.

- [ ] **Step 4: Commit**

```bash
git add cmd/delvop/root.go go.mod go.sum
git commit -m "feat: wire CLI to TUI — delvop launches"
```

---

## Task 13: Template System

**Files:**
- Create: `internal/templates/loader.go`, `internal/templates/builtin/solo.toml`, `internal/templates/builtin/fullstack.toml`
- Test: `internal/templates/loader_test.go`

- [ ] **Step 1: Write template test**

```go
package templates

import (
	"os"
	"path/filepath"
	"testing"
)

func TestLoadTemplate(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "test.toml")
	err := os.WriteFile(path, []byte(`
name = "test-template"
description = "A test template"

[[sessions]]
name = "worker"
provider = "claude"
model = "opus"
initial_prompt = "Do the work"
`), 0644)
	if err != nil {
		t.Fatal(err)
	}

	tmpl, err := LoadFile(path)
	if err != nil {
		t.Fatal(err)
	}
	if tmpl.Name != "test-template" {
		t.Errorf("expected name 'test-template', got %q", tmpl.Name)
	}
	if len(tmpl.Sessions) != 1 {
		t.Fatalf("expected 1 session, got %d", len(tmpl.Sessions))
	}
	if tmpl.Sessions[0].Provider != "claude" {
		t.Errorf("expected provider 'claude', got %q", tmpl.Sessions[0].Provider)
	}
}

func TestLoadBuiltins(t *testing.T) {
	templates, err := LoadBuiltins()
	if err != nil {
		t.Fatal(err)
	}
	if len(templates) < 1 {
		t.Error("expected at least 1 built-in template")
	}
}
```

- [ ] **Step 2: Run test, verify it fails**

```bash
go test ./internal/templates/ -v
```

- [ ] **Step 3: Create solo.toml**

```toml
name = "solo"
description = "Single agent working on a task"

[[sessions]]
name = "agent"
provider = "claude"
model = "opus"
initial_prompt = ""
```

- [ ] **Step 4: Create fullstack.toml**

```toml
name = "fullstack"
description = "Frontend + backend + test agents"

[[sessions]]
name = "frontend"
provider = "claude"
model = "opus"
initial_prompt = "You are working on the frontend"

[[sessions]]
name = "backend"
provider = "claude"
model = "opus"
initial_prompt = "You are working on the backend"

[[sessions]]
name = "tests"
provider = "claude"
model = "sonnet"
initial_prompt = "You are writing and running tests"
```

- [ ] **Step 5: Implement loader.go**

```go
package templates

import (
	"embed"
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/BurntSushi/toml"
)

//go:embed builtin/*.toml
var builtinFS embed.FS

type Template struct {
	Name        string            `toml:"name"`
	Description string            `toml:"description"`
	Sessions    []SessionTemplate `toml:"sessions"`
}

type SessionTemplate struct {
	Name          string `toml:"name"`
	Provider      string `toml:"provider"`
	Model         string `toml:"model"`
	InitialPrompt string `toml:"initial_prompt"`
}

func LoadFile(path string) (*Template, error) {
	data, err := os.ReadFile(path)
	if err != nil {
		return nil, err
	}
	var t Template
	if _, err := toml.Decode(string(data), &t); err != nil {
		return nil, fmt.Errorf("parse %s: %w", path, err)
	}
	return &t, nil
}

func LoadBuiltins() ([]*Template, error) {
	entries, err := builtinFS.ReadDir("builtin")
	if err != nil {
		return nil, err
	}
	var templates []*Template
	for _, entry := range entries {
		if !strings.HasSuffix(entry.Name(), ".toml") {
			continue
		}
		data, err := builtinFS.ReadFile(filepath.Join("builtin", entry.Name()))
		if err != nil {
			return nil, err
		}
		var t Template
		if _, err := toml.Decode(string(data), &t); err != nil {
			return nil, fmt.Errorf("parse builtin/%s: %w", entry.Name(), err)
		}
		templates = append(templates, &t)
	}
	return templates, nil
}

func LoadFromDir(dir string) ([]*Template, error) {
	entries, err := os.ReadDir(dir)
	if err != nil {
		if os.IsNotExist(err) {
			return nil, nil
		}
		return nil, err
	}
	var templates []*Template
	for _, entry := range entries {
		if !strings.HasSuffix(entry.Name(), ".toml") {
			continue
		}
		t, err := LoadFile(filepath.Join(dir, entry.Name()))
		if err != nil {
			return nil, err
		}
		templates = append(templates, t)
	}
	return templates, nil
}
```

- [ ] **Step 6: Run tests**

```bash
go test ./internal/templates/ -v
```

Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add internal/templates/
git commit -m "feat: template system with embedded builtins"
```

---

## Task 14: Integration — Full Build + Smoke Test

**Files:**
- Modify: various (fix any compilation issues)

- [ ] **Step 1: Full build**

```bash
cd /Users/meher/delvop
go mod tidy
go build .
```

Fix any compilation errors that arise from cross-package references.

- [ ] **Step 2: Run all tests**

```bash
go test ./... -v
```

Fix any test failures.

- [ ] **Step 3: Verify binary launches**

```bash
./delvop
```

Expected: TUI launches, shows empty state, responds to key presses (?, q).

- [ ] **Step 4: Test with tmux (if available)**

```bash
# In the TUI, press 'n', type 'test-agent', press enter
# Should create a tmux session and show it in the dashboard
```

- [ ] **Step 5: Final commit**

```bash
git add .
git commit -m "feat: delvop v0.1 — working TUI dashboard"
git tag v0.1.0
```

---

## Summary

| Task | Description | Dependencies |
|------|------------|--------------|
| 1 | Project scaffold + CLI | None |
| 2 | Config system | 1 |
| 3 | Provider interface + Claude | 1 |
| 4 | Session types + tmux bridge | 1 |
| 5 | Session manager | 2, 3, 4 |
| 6 | Hook engine | 1 |
| 7 | Notification router | 2 |
| 8 | Cost tracker + SQLite | 1 |
| 9 | TUI theme + styles | 1 |
| 10 | TUI messages + keys | 9 |
| 11 | TUI model + update + view | 5, 6, 7, 9, 10 |
| 12 | Wire CLI to TUI | 11 |
| 13 | Template system | 2 |
| 14 | Integration + smoke test | All |
